import { Routes } from '@angular/router';
import { Welcome } from './welcome';

/**
 * Definición de rutas del módulo de Bienvenida.
 */
export const WELCOME_ROUTES: Routes = [{ path: '', component: Welcome }];
